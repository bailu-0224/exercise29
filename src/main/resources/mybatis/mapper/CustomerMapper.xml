<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huadi.exercise29.reopsitory.CustomerRepository">
    <insert id="insert" parameterType="com.huadi.exercise29.domain.Customer">
        INSERT INTO customer (username, password, category)
            VALUES (#{username},#{password},#{category})
    </insert>
</mapper>